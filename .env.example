# Optional Google OAuth Web application client ID (public, not a client secret).
# Add http://localhost:5173 as an authorised JavaScript origin in Google Cloud.
# Enable the Google Calendar API. Read-only calendar and calendar-list scopes are used.
VITE_GOOGLE_CLIENT_ID=
